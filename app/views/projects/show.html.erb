<h1><%= @project.name %>のメンバー</h1>
<table>
  <tr>
    <th>名前</th>
    <th>種別</th>
    <th>メール</th>
  </tr>
  <% @projects.each do |user| %>
    <tr>
      <td><%= @project.user.name %></td>
      <td><%= @project.user.admin %></td>
      <td><%= @project.user.email %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'ユーザーを追加する', new_join_path(project_id: @project.id) %>
<%= link_to 'プロジェクトを削除する', project_path, method: :delete, data: { confirm: 'プロジェクトを削除してもよろしいですか？' }%>