<h1>ユーザーのマイページ</h1>

<table>
  <tr>
    <th>プロジェクト名</th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= project.name %></td>

    <td><%= link_to 'メンバー', project_path(project.id) %></td>
  </tr>
<% end %>
</table>
